---
- hosts: all
  become: true
  tasks:
    - name: install docker
      ansible.builtin.yum:
        name: docker
        state: latest
        
    - name: Start service docker
      ansible.builtin.systemd_service:
        name: docker
        state: started
